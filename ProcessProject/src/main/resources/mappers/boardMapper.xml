<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  

<mapper namespace="com.edu.mappers.boardMapper">



	<select id="getBoardList" resultType="com.edu.domain.BoardVO">
		SELECT
			BID, CATE_CD, TITLE, CONTENT, TAG, VIEW_CNT, REG_ID, REG_DT, EDIT_DT
		FROM
			TBL_BOARD
			<trim prefix="WHERE" prefixOverrides="AND|OR">
			<if test="searchType=='title' and keyword != null and keyword != '' ">
				AND TITLE like CONCAT('%', #{keyword}, '%')
			</if>
			<if test="searchType=='content' and keyword != null and keyword != '' ">
				AND CONTENT like CONCAT('%', #{keyword}, '%')
			</if>
			<if test="searchType=='reg_id' and keyword != null and keyword != '' ">
				AND reg_id like CONCAT('%', #{keyword}, '%')
			</if>	
		</trim>
		LIMIT #{startList}, #{listSize}

	</select>
	
	
	<!-- ■■■■■■■■■■■■■■■ 전체글의 개수를 읽어와 전체 페이지의 수를 계산  ■■■■■■■■■■■■■■■ -->
	
	<!-- 동적 SQL 문이 포함된다 : 주어딘 조건에 따라서 SQL문이 바뀌기 때문이다.
		trim : 하위 if에서 조건이 맞는 조건절이 있을 경우 where 키워드를 생성한다.
		if 	 : 조건이 맞을 경우 조건절을 생성한다. -->
	
	<select id="getBoardListCnt" resultType="int">
		SELECT
			count(*) as listCnt
		FROM
			TBL_BOARD
			<trim prefix="WHERE" prefixOverrides="AND|OR">
				<if test="keyword != null and keyword != '' ">
				
					<if test="searchType=='title'">
						AND TITLE like CONCAT('%', #{keyword}, '%')
					</if>
					<if test="searchType=='content'">
						AND CONTENT like CONCAT('%', #{keyword}, '%')
					</if>
					<if test="searchType=='reg_id'">
						AND reg_id like CONCAT('%', #{keyword}, '%')
					</if>
				</if>
			</trim>
	</select>

	
	<select id="getBoardContent" resultType="com.edu.domain.BoardVO" parameterType="com.edu.domain.BoardVO">
		SELECT
			BID, CATE_CD, TITLE, CONTENT, TAG, VIEW_CNT, REG_ID, REG_DT, EDIT_DT
		FROM
			TBL_BOARD
		WHERE
			BID = #{bid}
	</select>
	


	<insert id="insertBoard" parameterType="com.edu.domain.BoardVO">
		INSERT INTO TBL_BOARD (CATE_CD, TITLE, CONTENT, TAG, VIEW_CNT, REG_ID, REG_DT, EDIT_DT)
		VALUES (
			#{cate_cd}
			, #{title}
			, #{content}
			, #{tag}
			, 0
			, #{reg_id}
			, now()
			, now()
		)
	</insert>


	<update id="updateBoard" parameterType="com.edu.domain.BoardVO">
		UPDATE TBL_BOARD SET
			CATE_CD = #{cate_cd}
			, TITLE = #{title}
			, CONTENT = #{content}
			, TAG = #{tag}
			, EDIT_DT = now()
		WHERE
			BID = ${bid}
	</update>

	

	<delete id="deleteBoard" parameterType="int">
		DELETE FROM TBL_BOARD
		WHERE BID = #{bid}
	</delete>
	

	<update id="updateViewCnt" parameterType="com.edu.domain.BoardVO">

		UPDATE TBL_BOARD SET
			VIEW_CNT = VIEW_CNT + 1
		WHERE
			BID = #{bid}
	</update>

</mapper>
